[package]
name = "shared_data"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
chrono = { version = "0.4.31", default-features = false, features = ["alloc"] }
serde = { version = "1.0.188", default-features = false,  features = ["serde_derive"] }
serde_json = { version = "1.0.107", default-features = false, features = ["alloc"] }
sqlx = { version = "0.7.2", default-features = false, features = ["macros", "postgres", "runtime-tokio-rustls"], optional = true }

[features]
sqlx = ["dep:sqlx"]
